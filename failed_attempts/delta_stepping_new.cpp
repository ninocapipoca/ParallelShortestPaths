#pragma once
#include <iostream>
#include <vector>
#include <limits>
#include <utility>
#include <list>
#include <set>

#define DELTA     0.5 // for testing
using namespace std

// OBJECTS & VARIABLES WE HAVE
// requests
// edges & vertices
// weights for edges
// values for vertices
// buckets
// delta


vector<pair<int,int>> bucketList;

set<pair<int, int>> findRequests(list<pair<int,int>> V_list, bool light){
  // bool is whether we are looking for light or heavy vertices
  set<pair<int, int>> tempset;
  for (it = V_list.begin(); it != V_list.end(); it++){
    if 
  }

}
